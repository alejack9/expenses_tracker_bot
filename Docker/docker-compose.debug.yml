version: '3.4'

services:
  expensestrackbot:
    image: expensestrackbot
    build:
      context: ./..
      dockerfile: ./Docker/Dockerfile
    command: [ "python", "main.py", "--log=DEBUG" ]
    ports:
      - 5678:5678
    environment:
      - BOT_TOKEN
